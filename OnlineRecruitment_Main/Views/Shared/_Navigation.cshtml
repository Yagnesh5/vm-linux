<style>

    .qqq {
        width: 50px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        padding: 20px;
        font-size: 1.3rem;
        margin: 0;
        background: white;
        resize: horizontal;
    }

</style>

@using vrecruit.DataBase.EntityDataModel;
@using Microsoft.AspNet.Identity
@{
    string CurrentRoles = string.Empty;
    if (Session["CurrentRole"] == null)
    {
        Response.Redirect("~/Account/Login");
    }
    else
    {
        CurrentRoles = Session["CurrentRole"].ToString();
    }

}

<div id="navigation">
    <div class="profile-picture">
        @*<a href="@Url.Action("Index", "Dashboard")">
                <img src="~/Images/profile.jpg" class="img-circle m-b" alt="logo">
            </a>*@

        <div class="stats-label text-color">
            @if (Request.IsAuthenticated)
            {
                using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", }))
                {
                    @Html.AntiForgeryToken()
            <span class="font-extra-bold font-uppercase">
                Hello





                <div class="dropdown">
                    <a class="dropdown-toggle" href="#" data-toggle="dropdown">
                        <small class="text-muted qqq">  @User.Identity.GetUserName() ! <b class="caret"></b></small>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        @*<li><a href="contacts.html">Contacts</a></li>
                @*<li><a href="analytics.html">Analytics</a></li>
                    <li class="divider"></li>*@
                        <li><a href="~/Account/Profile">Profile</a></li>
                        <li><a href="#" id="ChangePassword">Change Password</a></li>
                        <li><a href="~/Account/LogOff">Logout</a></li>
                    </ul>
                </div>
                @*@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Profile", "Account", routeValues: null, htmlAttributes: new { title = "Profile"})*@


                                    
            </span>
        

                }
            }
            @*<div id="sparkline1" class="small-chart m-t-sm"></div>*@
            <div>
                @*<h4 class="font-extra-bold m-b-xs">
                        $260 104,200
                    </h4>
                    <small class="text-muted">Your income from the last year in sales product X.</small>*@
            </div>
        </div>
    </div>
    @if (Session["AccesspermissionList"] != null)
    {
        var MenuMaster = (List<OnlineRecruitment_Main.Models.MenuModels>)Session["AccesspermissionList"];
    <ul class="nav metismenu" id="side-menu">
        <li class="@Html.IsSelected(controller: "Master") Dashboard">
            <a class="acls_Dashboard" href="@Url.Action("Dashboard", "Master")"> <span class="nav-label">Dashboard</span></a>
        </li>
        @foreach (var item in MenuMaster)
        {
            if (item.IsView == true)
            {
                <li class="@Html.IsSelected(controller: "Master") @item.Entityname">
                    <a class="acls_@item.Entityname" href="@Url.Action(item.Entityname, "Master")"> <span class="nav-label">@item.Entityname</span></a>
                </li>
            }
        }
    </ul>
    }
</div>